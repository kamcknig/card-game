import { CardExpansionModule } from '@server-types/index.ts';

// Rising Sun card effects are intentionally deferred; this expansion currently provides setup-only model scaffolding.
const cards: CardExpansionModule = {};

export default cards;
