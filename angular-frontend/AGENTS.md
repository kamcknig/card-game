# Angular

Always use latest angular practices. This includes signals. getters in
templates and such should be avoided to keep change detection churn low.

Use standalone components whenever possible.

# CSS

Use latest sass/css rules and practices (use over import, etc);

prefer flex over grid without reason.
