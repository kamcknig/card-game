import { ArtifactEffectRegistrar } from '../../types.ts';

// Registers Renaissance artifact effects (none implemented yet).
export const registerArtifactEffects = (registerArtifactEffect: ArtifactEffectRegistrar) => {
  // Artifact effects will be registered here when implemented.
  void registerArtifactEffect;
};
