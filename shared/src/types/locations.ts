// Card locations, mats, and helpers used by gameplay and UI.
export { isLocationMat } from "../shared-types.ts";

export type {
  CardLocation,
  CardLocations,
  CardLocationSpec,
  Mats,
  PlayerMatMap,
} from "../shared-types.ts";
