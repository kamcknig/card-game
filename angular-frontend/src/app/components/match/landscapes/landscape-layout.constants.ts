import { EVENT_HEIGHT, EVENT_WIDTH, STANDARD_GAP } from '../../../core/app-contants';

// Intrinsic width of a single landscape card element (matches the EVENT_WIDTH CSS token).
export const LANDSCAPE_CARD_WIDTH_PX = EVENT_WIDTH;
// Intrinsic height of a single landscape card element (matches the EVENT_HEIGHT CSS token).
export const LANDSCAPE_CARD_HEIGHT_PX = EVENT_HEIGHT;
// Padding applied inside the landscape panel on all sides.
export const LANDSCAPE_PANEL_PADDING_PX = STANDARD_GAP;
// Gap between landscape card elements within the panel grid.
export const LANDSCAPE_PANEL_GAP_PX = STANDARD_GAP;
