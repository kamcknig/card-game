// Token ids used by Renaissance mechanics.
export const renaissanceTokenIds = {
  // Project ownership marker (one per player per configured project).
  cube: 'cube-token',
  // Tracks Sinister Plot counters on the project.
  sinisterPlot: 'renaissance:sinister-plot',
} as const;
