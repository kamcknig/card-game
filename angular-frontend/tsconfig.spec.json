/* To learn more about Typescript configuration file: https://www.typescriptlang.org/docs/handbook/tsconfig-json.html. */
/* To learn more about Angular compiler options: https://angular.dev/reference/configs/angular-compiler-options. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": [
      "jest"
    ],
    // moduleResolution: "bundler" (inherited from tsconfig.json) is incompatible
    // with Jest; override to "node" (classic Node.js resolution). "bundler" and
    // "node16"/"nodenext" are not compatible with Jest's CommonJS transform
    // pipeline. "node" resolution works with Jest's module mapper and does not
    // impose ESM/CJS strictness that would break Angular's ESM packages.
    "moduleResolution": "node",
    // The shared/ package uses explicit .ts extensions in re-exports for Deno
    // compatibility. allowImportingTsExtensions permits those imports;
    // rewriteRelativeImportExtensions rewrites .ts paths to .js in emitted
    // output so that noEmit is not required (unlike the paired noEmit approach).
    "allowImportingTsExtensions": true,
    "rewriteRelativeImportExtensions": true
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts"
  ],
  "exclude": [
    "src/test.ts"
  ]
}
